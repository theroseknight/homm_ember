import Ember from 'ember';
import ENV from '../config/environment';
export default Ember.Service.extend({
  leftHeroImageUrl:null,
  rightHeroImageUrl:null,
  leftHeroArmy:null,
  rightHeroArmy:null,
  preferenceHexagonGridVisible:null
});
